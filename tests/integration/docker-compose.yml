services:
  apache:
    container_name: apache
    image: owasp/modsecurity-crs:4-apache-202607160307@sha256:b2794a86d8b16909300b348634cc40e264a64075ecf5423e4e6f0fad4c0bda8c
    depends_on:
      - backend
    environment:
      SERVERNAME: "_default_"
      BACKEND: "http://backend:8080"
      MODSEC_RULE_ENGINE: "DetectionOnly"
      BLOCKING_PARANOIA: 4
      ERRORLOG: "/var/log/error.log"
      ACCESSLOG: "/var/log/access.log"
      MODSEC_AUDIT_LOG_FORMAT: Native
      MODSEC_AUDIT_LOG_TYPE: Serial
      MODSEC_AUDIT_LOG: "/dev/null"
      MODSEC_TMP_DIR: "/tmp"
      MODSEC_RESP_BODY_ACCESS: "On"
      MODSEC_RESP_BODY_MIMETYPE: "text/plain text/html text/xml application/json"
      COMBINED_FILE_SIZES: "65535"
      CRS_ENABLE_TEST_MARKER: 1
    ports:
      - 80:8080
    volumes:
      - ../../crs/rules:/opt/owasp-crs/rules:ro
      - ../../plugins:/opt/owasp-crs/plugins:ro
      - ../logs/apache:/var/log:rw
      - ../../crs-setup.conf.example:/etc/modsecurity.d/owasp-crs/crs-setup.conf.example
    networks:
      - crs-plugins-net
  
  nginx:
    container_name: nginx
    image: owasp/modsecurity-crs:4-nginx-202607160307@sha256:2051ff18b836c1d9bbc5c7754451c1687ea27352e497b89d0c9fc7e657861e07
    depends_on:
      - backend
    environment:
      SERVERNAME: "_"
      BACKEND: "http://backend:8080"
      MODSEC_RULE_ENGINE: DetectionOnly
      BLOCKING_PARANOIA: 4
      ERRORLOG: "/var/log/nginx/error.log"
      LOGLEVEL: "debug"
      ACCESSLOG: "/var/log/nginx/access.log"
      MODSEC_AUDIT_LOG_FORMAT: "Native"
      MODSEC_AUDIT_LOG_TYPE: "Serial"
      MODSEC_AUDIT_LOG: "/var/log/modsec_audit.log"
      MODSEC_RESP_BODY_ACCESS: "On"
      MODSEC_RESP_BODY_MIMETYPE: "text/plain text/html text/xml application/json"
      COMBINED_FILE_SIZES: "65535"
      CRS_ENABLE_TEST_MARKER: 1
    ports:
      - 80:8080
    volumes:
      - ../../crs/rules:/opt/owasp-crs/rules:ro
      - ../../plugins:/opt/owasp-crs/plugins:ro
      - ../logs/nginx:/var/log/nginx:rw
      - ../../crs/crs-setup.conf.example:/etc/modsecurity.d/owasp-crs/crs-setup.conf.example
    networks:
      - crs-plugins-net

  backend:
    image: ghcr.io/coreruleset/albedo:0.3.0@sha256:843ed01d28f48b594dcc0278ea9403175a0bf40ec065432040b796f589e89507
    networks:
      - crs-plugins-net
  
volumes:
  logs:
networks:
  crs-plugins-net:
    driver: bridge
